#!/bin/sh

# These env variables run before the mix release is started.
HOSTNAME=$(hostname -f)

export DNS_CLUSTER_QUERY=$HOSTNAME
export RELEASE_DISTRIBUTION="name"
export RELEASE_NODE="helloworld@${HOSTNAME}"
